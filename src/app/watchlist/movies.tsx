import { WatchlistListScreen } from '@/components/watchlist-list-screen';

export default function MovieWatchlistScreen() {
  return <WatchlistListScreen mediaType="movie" emptyMessage="Movies you plan to watch will show up here." />;
}
